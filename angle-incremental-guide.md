# Angle Incremental 初心者ガイド

このガイドは、はじめて **Angle Incremental** を遊ぶ人向けの説明です。細かい数式や内部仕様を知りたい場合は [angle-incremental-spec.md](angle-incremental-spec.md) を参照してください。

## 1. どんなゲームか

画面中央の図形が **The Angle** です。点が図形の外周を回り、上の頂点である **核** に着くとスコアが増えます。

最初は小さなスコアを集め、通常強化を買い、やがて大きなリセットを使って次の段階へ進みます。リセットは進行を戻す代わりに、次の周回を強くするための仕組みです。

大まかな進行順は次の通りです。

```text
通常強化 -> Generation -> Core Boost -> Infinity -> IA / Tower / Infinity Challenge / Break Infinite Cap
```

## 2. 最初に見るところ

| 表示 | 意味 |
| --- | --- |
| スコア | 通常強化を買うための基本リソースです。 |
| 核到達時の獲得量 | 点が一番上の頂点に着いた時にもらえるスコアです。 |
| 頂点通過ごとの増加 | 点が頂点を通るたび、次の獲得量がどれだけ伸びるかです。 |
| 1周の時間 | 点が図形を1周する速さです。短いほど早く進みます。 |
| ラップ速度 | 周回速度の倍率です。 |

まずはスコアを貯めて、左側の通常強化を買います。

## 3. 通常強化の選び方

通常強化は3種類あります。

| 強化 | 何が起きるか | 迷った時 |
| --- | --- | --- |
| 周回速度 | 点が早く回ります。 | 待ち時間が長いなら買います。 |
| 角の追加 | 図形の頂点が増え、獲得式が強くなります。 | 買えるなら強いことが多いです。 |
| 頂点獲得量 | 頂点を通った時の伸びが増えます。 | 獲得量の伸びが鈍い時に買います。 |

`全購入` ボタンは、買える通常強化をまとめて購入します。序盤は細かく悩まず、買えるものを買って進めて大丈夫です。

## 4. Generation

累計スコアが **1,000,000** に届くと **Generation** が使えるようになります。

Generationを実行すると、スコアや通常強化はリセットされます。その代わり、次の周回でスコアを伸ばしやすくなる恒久ボーナスを得ます。

リセットされるもの:

- 現在のスコア
- 通常強化
- 今回のGeneration中に得たスコア

残るもの:

- Generation回数
- Generationで得た倍率やコスト補正
- 累計プレイ時間や実績

Generationは怖いリセットではなく、次の周回を強くするための前進です。

## 5. Core Boost

所持スコアが **1.00e20** に届くと **Core Boost** ができます。

Core BoostはGenerationより上位のリセットです。Generationで伸ばした進行もリセットされますが、スコア獲得の土台が強くなります。

Core Boost後は、また通常強化とGenerationを進め直します。最初は遠く感じますが、Core Boostを重ねると次の周回がかなり早くなります。TC2をクリアすると、Floor 5以降のTower階数に応じて次のCore Boost要求量の増え方が緩やかになります。

## 6. Infinity

所持スコアが **1.80e308** に届くと **Infinity** に到達します。

初回Infinityは自動で実行されます。2回目以降は、条件を満たした状態で自分で実行できます。

Infinityで得る主なものは **Infinity Point (IP)** です。IPは **Infinity Upgrade (IU)** の購入に使います。IUはInfinityでリセットされない恒久強化です。

Infinity後に残るもの:

- Infinity回数
- IP
- 購入済みのIU
- クリア済みのIC
- 実績
- 設定と統計

Infinity後に戻るもの:

- スコア
- 通常強化
- Generation
- Core Boost
- Infinite Score

## 7. Infinity Upgrade

Infinity Upgradeは、Infinity後の主な成長要素です。

序盤の目安:

1. IU 1-1と1-2を買う。
2. IU 2-1で周回速度を上げる。
3. IU 3-1と3-2でGenerationを強くする。
4. IU 4-1でInfinity Challengeを解放する。

後半のIUでは、Infinity自動化、IP獲得式の変更、リセット後のCore Boost開始、所持スコアの指数化などが解放されます。

IU14-1はIU13-1の後段にあり、IU11-2のInfinity回数ハードキャップを10000から30000へ延長します。

## 8. Infinity Challenge

Infinity Challenge (IC) は、制約付きでInfinity到達を目指す挑戦です。IU 4-1で解放されます。

ICを開始すると、何かが弱くなったり禁止されたりします。その状態でInfinityに到達するとクリアです。クリア報酬は恒久的に残ります。

例:

- IC1: 獲得式が悪くなります。クリアするとその弱体を取り除く報酬を得ます。
- IC3: ラップ速度が弱くなります。クリアするとラップ速度が強くなります。
- IC7: スコアが高いと通常強化を買えません。クリアすると通常強化購入時にスコアを消費しなくなります。
- IC8: 頂点数は3固定になります。角追加アップグレードは角を増やさず、頂点通過ごとの増加とスコア獲得量を伸ばす専用効果になります。クリアするとGRスコア倍率の伸び方が変わり、その倍率がIP獲得量にも効くようになります。

挑戦中に詰まったら、ICを中止して別の強化を進めてから戻ってください。

## 9. Break Infinite Cap、Infinite Angle、Tower

**Break Infinite Cap** は、スコアが **1.00e350** に届くと実行できます。実行すると、Infinity条件を超えた後の強いスコア制限が外れます。

**Infinity Angle (IA)** は、**1.00e20 IP** を一度支払うと解放される、Infinity内の独立した図形です。IAは常に進行し、IAの核到達で **Infinity Score** を稼ぎます。Infinity Scoreの `^0.3` が通常の頂点獲得量に乗算されます。

IAには周回速度、角の追加、頂点獲得量の専用強化があります。初回コストはそれぞれ **1.00e20 IP / 2.40e20 IP / 3.60e20 IP** です。これらはIPで購入し、Infinityしても残ります。IAタブの `IA強化を全購入` は、周回速度→角の追加→頂点獲得量の順に買える分をまとめて購入します。InfinityするとInfinity ScoreとIAの現在の周回進行だけがリセットされます。IAにはGeneration、Core Boost、Infinity Challenge、Infinity Upgradeの補正はありません。

**Tower** は **1.00e50 IP** から建設できます。TowerはInfinityでリセットされず、階数ごとに通常スコア累乗へ `+^0.05` の補正が加わります。Floor 3、5、8、12でそれぞれTC1〜TC4が解放され、その先の建設には対応するTCのクリアが必要です。

TC1の目標は **1e1000 Score** で、クリアするとFloor 3を基準に1階層ごとにInfinity Scoreの累乗へ`+0.077`が加わります。TC2の目標は **1e3000 Score** で、クリア後はFloor 5を基準にCore Boost要求量の生指数が1階層ごとに`-0.03`されます。生指数が`1.50`未満になると強いソフトキャップがかかりますが、効果は固定されずに少しずつ伸び続けます。TC3はFloor 8で解放され、**1e5000 Score** が目標です。TC3中はScore獲得とInfinity Score生成を`^0.001`／`^0.1`から開始し、Infinity回数600,000で`^0.8`／`^0.5`まで緩和します。クリア後はFloor 8以降の追加階層ごとにSpeed・Vertex・Gainの有効購入数が`×1.05`されます。TC4はまだ未実装です。

### オフライン進行

Settingsタブの「オフライン進行」で、離席中の進行を有効／無効に切り替えられます。古いセーブなどに設定項目がない場合は有効として扱われ、明示的に無効にした設定は保存・再読み込み後も維持されます。有効時は保存してから戻るまでの時間に応じてゲーム時間、Infinity進行、自動化が進みますが、離席時間は実プレイ時間には含まれません。無効時は離席中のゲーム進行、自動化、ゲーム時間を進めず、基準時刻だけを現在時刻へ更新します。無効にしていた時間は、後から有効に戻しても遡って処理されません。オフラインティック数は有効時のシミュレーションにのみ使用し、離席時間を何回の粗い更新に分けるかを1,000〜1,000,000の範囲で設定できます。処理は最大1,000ティックまたは約8msごとにイベントループへ戻るため、100万ティックでも進捗を確認できます。

サーバー時刻が利用できる場合は7日を超える離席時間も処理します。サーバー時刻を利用できない場合は、時計の安全対策として最大7日までに制限します。通常Infinity周回の最高Infinity回数/秒は保存され、Infinity自動化などの条件が揃った復帰時には、通常シミュレーションで得た分を除いたInfinity回数を直接集約します。Time Fluxは一時的に削除されており、現行版のオンライン速度は常にx1です。過去のTime Flux値は古いセーブとの互換性のため保持されますが、ゲーム進行には使われません。

## 10. 実績

実績は37個あります。実績を取るたびに、頂点通過ごとの増加に少しずつ倍率がかかります。

一部の実績には追加報酬があります。

- GR倍率を強くする。
- IP獲得量を増やす。
- GR/CB自動化を解放する。
- GRコストをさらに軽くする。

実績はどのリセットでも失われません。

## 11. 自動化

ゲームが進むと自動化が増えます。

| 解放 | 自動化 |
| --- | --- |
| IU 1-2 | 通常強化の自動購入 |
| IU 4-1 | IC自動完了 |
| 実績19 | GenerationとCore Boostの自動実行 |
| IU 8-1 | Infinity自動実行 |

自動化は便利ですが、しきい値が合わないと早すぎるリセットになることがあります。進み方が変だと感じたら、Automationタブのしきい値を見直してください。

## 12. 上部バー、ニュース、統計

画面上部のバーは設定で切り替えられます。

- ニュース
- 資源量
- 進捗状況
- ブランク
- 隠す

Statisticsタブでは、ゲーム時間と、オンライン中だけを数える実プレイ時間を確認できます。総プレイ時間、現在のInfinity周回時間、最速Infinityはゲーム時間と実プレイ時間が別々に表示されます。Time Flux休止前のセーブや履歴には、以前の速度設定の影響が残る場合があります。

Infinity履歴には、ゲーム時間・実プレイ時間、到達スコア、獲得IPが残ります。旧セーブ由来で実プレイ時間がない履歴は「不明」と表示されます。自動化が進むと、画面上ではスコアが一瞬でリセット後の値に戻って見えることがあります。その場合は統計やIP量を見ると、実際に進んでいるか確認できます。

## 13. よくある表示

### `1.00e20` のような表示

`e` は10の何乗かを表します。

```text
1.00e20 = 1.00 * 10^20
```

数字が大きくなるゲームなので、途中からこの表記が中心になります。

### リセット後にスコアが251になる

IU 5-2を買うと、リセット後にスコア100から始まります。さらにIU 10-2を買うと、所持スコアが `^1.2` されます。

そのため、内部的な開始スコア100が表示上はおよそ251になります。これは異常ではありません。

### 「最速Infinity」が短すぎる

Infinity時間が短く見える場合は、実プレイ時間、IP、Infinity回数、過去10回のInfinity記録も合わせて確認してください。

## 14. セーブ

進行はブラウザのローカルストレージに保存されます。設定タブからセーブコードを書き出し、別の環境へ移すこともできます。

セーブコードは `ANGLE_SAVE_V2:` で始まります。大きな進行を失わないため、アップデート前や環境を変える前にはセーブコードを書き出しておくと安全です。

同じゲームを複数のタブで開いていると、セーブ競合が検出されることがあります。その場合は現在のタブの状態を退避してから最新のセーブを読み込みます。退避に失敗したときは、Settingsからセーブコードを書き出して復旧してください。競合を避けるため、通常は1つのタブだけで遊ぶことをおすすめします。
