const fs = require("node:fs");
const path = require("node:path");
const { loadRuntime } = require("./runtime-harness-esm.js");
const candidatePath = path.join(__dirname, "..", "src", "main.js");
const reportPath = path.join(__dirname, "..", "first-infinity-strategy-search-report.json");
const ACTION_INTERVAL_SECONDS = 0.25;
const BASELINE_CUTOFF_SECONDS = 2125.25;
function currentLog(runtime,key){return runtime.currentLog10ForValue(runtime.state[key],runtime.state[`${key}Log10`]);}
function installPreInfinityAggregateBatch(runtime){runtime.processManyVertices=function(start,end){const count=end-start+1;if(count<=0)return false;const increase=runtime.vertexGainIncrease();const events=runtime.coreVertexIndices().map((index)=>{const offset=((index-(start%runtime.state.vertices))+runtime.state.vertices)%runtime.state.vertices;const hits=offset>=count?0:Math.floor((count-1-offset)/runtime.state.vertices)+1;return{firstStep:offset+1,hits};}).filter((event)=>event.hits>0);if(events.length){let earned=0,totalHits=0,lastStep=0;for(const event of events){earned+=runtime.sumCoreHitGains(event.firstStep,event.hits,increase);totalHits+=event.hits;lastStep=Math.max(lastStep,event.firstStep+(event.hits-1)*runtime.state.vertices);}const fallback=runtime.log10Value(Math.max(totalHits,1))+runtime.finalScoreGainFromBaseLog10(runtime.gainAfterIncreaseLog10(increase,lastStep));if(runtime.addScore(earned,Number.isFinite(earned)?runtime.log10Value(earned):fallback))return true;}runtime.addCurrentGain(increase*count);return false;};}
function buyByMode(debug,mode){if(mode==="all")return debug.buyAllUpgrades({refresh:false,save:false});if(mode==="speed-gain")return debug.buyAllUpgrades({refresh:false,save:false,allowVertex:false});if(mode==="speed-vertex")return debug.buyAllUpgrades({refresh:false,save:false,allowGain:false});if(mode==="vertex-gain")return debug.buyAllUpgrades({refresh:false,save:false,allowSpeed:false});throw new Error(`Unknown purchase mode: ${mode}`);}
function policyDepth(p,cb,gr){if(p.kind==="fixed")return p.depth;if(p.kind==="by-core")return p.depths[Math.min(cb,p.depths.length-1)];if(p.kind==="late-split")return cb<4?p.baseDepth:(gr<p.switchAfter?p.earlyDepth:p.lateDepth);throw new Error(`Unknown policy kind: ${p.kind}`);}
function allowGeneration(p,cb,gr){return p.maxGenerationAfterCore4===undefined||cb<4||gr<p.maxGenerationAfterCore4;}
async function simulate(p,cutoff=BASELINE_CUTOFF_SECONDS){const{debug,runtime}=await loadRuntime(candidatePath);const state=debug.state;state.showFloatingText=false;state.lightEffects=true;runtime.saveGame=()=>{};runtime.updateUi=()=>{};installPreInfinityAggregateBatch(runtime);let time=0,next=0,buyAllCalls=0,generations=0,coreBoosts=0;const timeline=[];while(time<cutoff&&state.infinityCount===0){const target=Math.min(next,cutoff),dt=Math.max(0,target-time);if(dt>0){debug.update(dt);time=target;}if(time+1e-9>=next){buyByMode(debug,p.purchaseMode||"all");buyAllCalls++;const score=runtime.currentScoreLog10(),coreReq=runtime.coreBoostRequirementLog10(),coreTarget=coreReq+(p.coreDelayLog||0),generationReq=runtime.generationRequirementLog10(),depth=policyDepth(p,state.coreBoostCount,state.generationCount),generationScore=currentLog(runtime,"generationScore"),canCore=runtime.canCoreBoost()&&score>=coreTarget,canGeneration=runtime.canRunGeneration()&&allowGeneration(p,state.coreBoostCount,state.generationCount)&&generationScore>=generationReq+depth;if(p.priority==="generation"&&canGeneration){debug.runGeneration();generations++;timeline.push({type:"GR",time,coreBoostCount:state.coreBoostCount,generationCountBefore:state.generationCount-1,requirementLog10:generationReq,scoreLog10:generationScore,depth});}else if(canCore){debug.runCoreBoost();coreBoosts++;timeline.push({type:"CB",time,coreBoostCountBefore:state.coreBoostCount-1,requirementLog10:coreReq,targetLog10:coreTarget,scoreLog10:score});}else if(canGeneration){debug.runGeneration();generations++;timeline.push({type:"GR",time,coreBoostCount:state.coreBoostCount,generationCountBefore:state.generationCount-1,requirementLog10:generationReq,scoreLog10:generationScore,depth});}next+=ACTION_INTERVAL_SECONDS;}if(target===cutoff)break;}const run=state.lastInfinityRuns[0]||null;return{name:p.name,policy:p,reachedInfinity:state.infinityCount>0,firstInfinitySeconds:run?run.time:null,elapsedSeconds:time,finalScoreLog10:runtime.currentScoreLog10(),buyAllCalls,generations,coreBoosts,resetTimeline:timeline};}
function fixed(name,depth,extra={}){return{name,kind:"fixed",depth,purchaseMode:"all",coreDelayLog:0,priority:"core",...extra};}
function createCandidates(){const candidates=[fixed("reference_fixed_d20",20)];for(const depth of[10,12,14,16,18,19,21,22,24,26,28,30,35,40,45,50,60])candidates.push(fixed(`fixed_d${depth}`,depth));for(const coreDelayLog of[2,5,10,20])candidates.push(fixed(`d20_core-delay-${coreDelayLog}`,20,{coreDelayLog}));for(const depth of[24,30])candidates.push(fixed(`d${depth}_core-delay-5`,depth,{coreDelayLog:5}));for(const purchaseMode of["speed-gain","speed-vertex","vertex-gain"])candidates.push(fixed(`d20_${purchaseMode}`,20,{purchaseMode}));for(let maximum=0;maximum<=8;maximum++)candidates.push(fixed(`d20_limit-after-cb4_${maximum}`,20,{maxGenerationAfterCore4:maximum}));for(const stage of[1,2,3,4])for(const depth of[12,16,24,30,40]){const depths=[20,20,20,20,20];depths[stage]=depth;candidates.push({name:`by-core_stage${stage}_d${depth}`,kind:"by-core",depths,purchaseMode:"all",coreDelayLog:0,priority:"core"});}for(const switchAfter of[1,2])for(const earlyDepth of[15,20])for(const lateDepth of[30,40,60])candidates.push({name:`late-split_after${switchAfter}_early${earlyDepth}_late${lateDepth}`,kind:"late-split",baseDepth:20,switchAfter,earlyDepth,lateDepth,purchaseMode:"all",coreDelayLog:0,priority:"core"});return candidates;}
async function runFirstInfinityStrategySearchAudit(){const candidates=createCandidates(),results=[];for(const candidate of candidates)results.push(await simulate(candidate));const winners=results.filter((result)=>result.reachedInfinity).sort((a,b)=>a.firstInfinitySeconds-b.firstInfinitySeconds),report={assumptions:{newSave:true,infinityUpgradesOrAutomation:false,buyAllIntervalSeconds:ACTION_INTERVAL_SECONDS,baselineCutoffSeconds:BASELINE_CUTOFF_SECONDS,coreBoostPolicy:"Core Boost is immediate unless a candidate explicitly adds a log-delay.",generationPolicy:"Generation occurs when Generation Score reaches the policy's requirement + depth threshold.",candidateCount:candidates.length,candidateFamilies:["fixed depth","Core Boost delay","selective purchases","post-CB4 Generation limit","per-Core-Boost depth","late-phase split depth"],persistence:"Disabled only in the audit runtime; it does not affect game state."},reference:results.find((result)=>result.name==="reference_fixed_d20"),best:winners[0]||null,winners,nonWinners:results.filter((result)=>!result.reachedInfinity)};fs.writeFileSync(reportPath,`${JSON.stringify(report,null,2)}\n`);console.log("FIRST_INFINITY_STRATEGY_SEARCH",JSON.stringify({candidates:candidates.length,referenceSeconds:report.reference&&report.reference.firstInfinitySeconds,best:report.best&&{name:report.best.name,seconds:report.best.firstInfinitySeconds,generations:report.best.generations,coreBoosts:report.best.coreBoosts},beatingReference:winners.filter((result)=>result.firstInfinitySeconds<BASELINE_CUTOFF_SECONDS).length}));return report;}
module.exports={runFirstInfinityStrategySearchAudit};
